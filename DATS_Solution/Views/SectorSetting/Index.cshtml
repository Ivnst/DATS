@model IEnumerable<DATS.Sector>

@{
    ViewBag.Title = "Справочник секторов: Список";
    Layout = "~/Views/Shared/_SettingLayout.cshtml";
}

<div class="btn-group" style="margin: 10px 0 10px">
    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
        @ViewBag.ChooseStadium <span class="caret"></span>
    </button>
    <ul class="dropdown-menu" role="menu">
        @foreach (var sv in ViewBag.Stadiumes)
        {
            <li><a href="/Settings/Sectors?sid=@sv.Id">@sv.Name</a></li>
        }

    </ul>
    <button data-toggle="modal" data-target="#myModal" href="/SectorSetting/Create/@ViewBag.ChooseStadiumId" class="btn btn-primary" style="margin: 0 0 0 10px">Добавить сектор</button>
</div>


<table class="table table-striped table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            Редактирование мест
        </th>
        <th>
            
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a href="/Sector/Configure?sid=@item.Id" class="btn btn-success">Редактировать места</a>
                <a href="/Sector/Configure?sid=@item.Id" class="btn btn-info">Создать копию</a>
            </td>
            <td>
                <button data-toggle="modal" data-target="#myModal" href="/SectorSetting/Edit/@item.Id" class="btn glyphicon glyphicon-pencil"></button>
                <button data-toggle="modal" data-target="#myModal" href="/SectorSetting/Delete/@item.Id" class="btn glyphicon glyphicon-trash"></button>
            </td>
        </tr>
    }

</table>

<!-- Modal-->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
        </div>
    </div>
</div>
